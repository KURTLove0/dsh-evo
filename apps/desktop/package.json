{
  "name": "@deepseek-ai/dsh-desktop",
  "description": "Desktop shell for the dsh Web GUI: an Electron main process that hosts the real `dsh web` composition as a child process and loads its loopback URL in a BrowserWindow",
  "version": "0.1.1-rc.1",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deepseek-ai/deepseek-harness.git",
    "directory": "apps/desktop"
  },
  "type": "module",
  "main": "lib/main.js",
  "files": [
    "lib/*.js"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc -b && tsdown",
    "dev": "pnpm run build && electron ."
  },
  "devDependencies": {
    "electron": "^42.0.0"
  }
}
